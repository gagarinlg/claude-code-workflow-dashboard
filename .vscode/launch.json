{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Claude Code Workflow Dashboard (Extension Host)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}", "--disable-extensions"],
      "preLaunchTask": "npm: build"
    }
  ]
}
